---
description: Generate a conventional commit message for staged changes.
agent: build
---

Write a concise conventional commit message for the staged changes. Use present tense. Keep the subject under 72 characters.

!`git diff --cached --stat`
!`git diff --cached`
